# slist
programs related to processing of Steve Koepke's List
Html page is super out of date and irrelevant moved onto Linux-based bash/python accessible as source code in the slist folder in this repo

# 11-12-2025 
Initial upload of the slist bash project that i've been slowly developing with Open AI coding tools let's call it version 0.9 all source code currently, not fully functioning bash program with installation, build and run directly from source code, you can run at slist/bin$ bash main.sh you can also run $bash dependencies-checker.sh to see if you have all the dependencies... more detailed explanation of usage to come

Dependencies: 
Bash ≥ 4.3
Python 3 ≥ 3.5 (stdlib only)
xmlstarlet ≥ 1.6.1
GNU coreutils ≥ 8.21 (printf, fold, tr, od)
sed ≥ 4.2, awk (mawk or gawk), grep ≥ 2.20
util-linux ≥ 2.20 (stty), ncurses-bin ≥ 5.9 (tput)
curl ≥ 7.38 
